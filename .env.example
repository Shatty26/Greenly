# No client-side environment variables are required.
#
# The chatbot's Gemini access runs through a Netlify Function
# (netlify/functions/chat.mts) backed by Netlify AI Gateway, which injects
# the provider credentials automatically at runtime. There is no API key to
# set for the chatbot to work.
